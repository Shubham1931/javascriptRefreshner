const name = "shubham"
let num = 8;
var fruit = "apple";
city = "meerut"

// if we try to change const it will not happen because const is unchangeable
// the problem with var is the it is not the scope specific
// so the problem with var is solved by the let keyword
// name = "hey" it will throw the error
 num  = 5;
 fruit = "banana"
 city = "mrt"
 console.table([num,fruit,city]);
 
