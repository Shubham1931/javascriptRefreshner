const arr  = ["shubham",23,4,"hello"]
console.log(arr);
const myArr = new Array(1,2,3,4)
myArr.pop() 
console.log(myArr);
// slice and splice

// slice - return the part of the array but not manipulate the array
// splice - extract the part of the array and manipulate the array